# Llama.cpp Daily News — 2026-06-28

> Auto-generated by pi-agent-core + DeepSeek | ggml-org/llama.cpp
> Model matrix: 4 models × 3 GPU setups

---

## 📊 Summary

| Metric | Count |
|--------|-------|
| PRs analyzed | 2 |
| Backend PRs | 1 |
| Bug fixes | 0 |
| Performance PRs | 1 |
| Model Support | 0 |
| Server/API | 1 |
| CLI/Tooling | 0 |
| Documentation | 0 |
| Other | 0 |

---

## 🔍 PR Details

### [Backend/Performance] — PR #25069: opencl: flash attention improvement

**Merged:** 2026-06-27 | **Author:** @wanghqc (Qualcomm)

**Summary:** A comprehensive rework of the Flash Attention (FA) kernel for the OpenCL backend, improving both precision and performance while adding support for quantized KV cache. The rewrite targets Adreno GPUs found in flagship Android devices and Windows on Snapdragon (WoS) platforms with X1 and X2 GPUs. It works well with models using `head_dim` of 64 (e.g., gpt-oss-20b) and reduces data traffic for larger `head_dim` models. This is a major upgrade for mobile and Snapdragon-adjacent users — anyone running llama.cpp through the OpenCL backend on Qualcomm hardware should see significant FA performance improvements and better memory efficiency with quantized KV cache.

**Affected areas:** OpenCL backend, Flash Attention kernel, quantized KV cache, Adreno GPU, Qualcomm Snapdragon

---

### [Server/API] — PR #25078: logs: reduce v2

**Merged:** 2026-06-28 | **Author:** @ggerganov

**Summary:** Further reduces the default verbosity of llama.cpp log output by migrating many informational (`LOG_INF`) and warning (`LOG_WRN`) messages down to trace-level (`TRC`) across the common library, speculative decoding engine, and server codebase. Subsystem-specific log macros (`COM_*`, `SPC_*`) were introduced for `common.cpp`, `speculative.cpp`, and server files to keep logs organized while reducing noise. The `n_ctx_seq < n_ctx_train` warning was downgraded to a simple info message. Users who relied on verbose startup logs can use `-lv 4` to restore the original verbosity. This affects all llama-server, llama-cli, and spec-decoding users — expect significantly cleaner default output.

**Affected areas:** logging system, common library, speculative decoding, server, -lv flag

---

*Report generated 2026-06-28T09:05:27.840Z | Next update: tomorrow*
