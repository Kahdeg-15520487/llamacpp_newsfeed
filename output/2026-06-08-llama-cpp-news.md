# Llama.cpp Daily News — 2026-06-08

> Auto-generated by pi-agent-core + DeepSeek | ggml-org/llama.cpp
> Model matrix: 4 models × 3 GPU setups

---

## 📊 Summary

| Metric | Count |
|--------|-------|
| PRs analyzed | 27 |
| Backend PRs | 6 |
| Bug fixes | 6 |
| Performance PRs | 3 |
| Model Support | 4 |
| Server/API | 2 |
| CLI/Tooling | 3 |
| Documentation | 0 |
| Build/CI | 2 |
| Other | 1 |

---

## 🔍 PR Details

### [Backend] — PR #23991: vulkan: Use cm2 decode_vector for mul_mat_id B matrix loads

**Merged:** 2026-06-08 | **Author:** @jeffbolznv

**Summary:** Optimizes MoE (Mixture of Experts) matrix loads in the Vulkan backend by using `cm2 decode_vector` instructions to enable vec4 loads of B elements, combined with increasing BK to 64. Together these yield significant speedups — up to ~18% for prompt processing on MoE models (e.g., Nemotron 30B A3B went from 8126 to 9658 t/s on an RTX 5090). All Vulkan users with MoE models benefit automatically with no flag changes needed.

**Affected areas:** Vulkan, ggml

---

### [Backend] — PR #24087: CUDA: enroll mul_mat_vec_q_moe into pdl

**Merged:** 2026-06-05 | **Author:** @ORippler

**Summary:** Enrolls the `mul_mat_vec_q_moe` kernel into the persistent data loading (PDL) system for CUDA. This gives a small (~1%) performance boost at microbatch sizes between 1 and 8 during prompt processing of MoE models. The improvement is automatic for CUDA users with no configuration changes needed.

**Affected areas:** CUDA, ggml

---

### [Backend] — PR #21845: sycl: port multi-column MMVQ from CUDA backend (~45% speculative decoding speedup on Intel Arc)

**Merged:** 2026-06-05 | **Author:** @masonmilby

**Summary:** Ports the multi-column MMVQ (multi-mat-vec) optimization from the CUDA backend to SYCL. Previously, speculative decoding on SYCL was slower than single-token prediction because MMVQ launched a separate kernel per column. Now weights are read once and all columns are computed in a single dispatch, yielding ~45% speedup for speculative decoding on Intel Arc GPUs. SYCL users benefit automatically with no flag changes.

**Affected areas:** SYCL, ggml

---

### [Backend] — PR #24129: HIP: add gfx1152 and gfx1153 to RDNA3.5

**Merged:** 2026-06-08 | **Author:** @harkgill-amd

**Summary:** Adds `gfx1152` and `gfx1153` GPU architecture targets to the RDNA3.5 macro in the HIP/CUDA backend. This resolves corrupted output reported by Rock users on these newer AMD GPU variants. AMD GPU users with these architectures will now get correct inference results without manual workarounds.

**Affected areas:** HIP/ROCm, ggml

---

### [Backend] — PR #24186: vulkan: check coopmat2 features before reporting support

**Merged:** 2026-06-06 | **Author:** @0cc4m

**Summary:** Fixes the Vulkan backend incorrectly advertising NV_coopmat2 matrix core support on platforms where the driver (e.g., Mesa ANV/RADV) exposes the extension but the device doesn't actually support the cooperative matrix features. Previously, Intel Arc A770 was erroneously reported as having coopmat2 support. This change prevents incorrect dispatch paths from being selected.

**Affected areas:** Vulkan, ggml

---

### [Backend] — PR #24220: metal: fix im2col 1D case (audio models)

**Merged:** 2026-06-08 | **Author:** @ngxson

**Summary:** Fixes a regression introduced by PR #23901 that broke the conv1d operation on Apple Metal, which is used by audio models (Ultravox, Qwen2.5-Omni, Voxtral). Audio inference on Apple Silicon devices now works correctly again. Users running audio/multimodal models on macOS benefit from this fix.

**Affected areas:** Metal, ggml

---

### [Model Support] — PR #23398: llama: add Gemma4 MTP

**Merged:** 2026-06-07 | **Author:** @am17an

**Summary:** Adds Multi-Token Prediction (MTP) / speculative decoding support for Gemma 4 models (both dense and MoE variants). The dense model sees >2x speedup on average, while the MoE variant may not show improvement on all systems. Multi-GPU setups work but may need `--spec-draft-device` with `-sm layer`. Users with Gemma 4 models can now use speculative decoding for faster generation, reproducing the ~87% AIME-26 accuracy advertised by the Gemma team.

**Affected areas:** model, server, python, examples

---

### [Model Support] — PR #23545: model: Granite4 Vision

**Merged:** 2026-06-05 | **Author:** @gabe-l-hart

**Summary:** Adds support for the `Granite4VisionForConditionalGeneration` multimodal architecture, targeting IBM's granite-vision-4.1-4b and granite-4.0-3b-vision models. This new mtmd architecture handles vision-language tasks. Users can now run these granite vision models locally through llama.cpp's multimodal pipeline.

**Affected areas:** model, examples, python

---

### [Model Support] — PR #24060: hparams: refactor `hparams.n_layer`

**Merged:** 2026-06-05 | **Author:** @ggerganov

**Summary:** Refactors the model layer enumeration logic to clearly distinguish between `n_layer_all` (all layers from the model file, including extras like `nextn`) and `n_layer()` (actual model layers). This improves correctness for models with extra layers (e.g., MTP heads) and fixes edge cases where off-by-one errors could occur. Some models may have been broken by this change — users should test their specific model configurations.

**Affected areas:** model

---

### [Model Support] — PR #21858: mtmd: support "frame merge" for qwen-vl-based models

**Merged:** 2026-06-06 | **Author:** @ngxson

**Summary:** Adds frame merge support for Qwen-VL-based models by leveraging the conv3d architecture these models use for video input. This is an intermediate step toward full video input support in the multimodal pipeline. No end-to-end video pipeline is provided yet, but this lays the groundwork for future video understanding capabilities.

**Affected areas:** mtmd, examples

---

### [Bug Fix] — PR #24068: fix: handle hybrid models where layer 0 has no attention heads

**Merged:** 2026-06-04 | **Author:** @AdityaKulshrestha

**Summary:** Fixes a model loading crash for hybrid architectures like Plamo2 that alternate between Mamba (SSM, n_head=0) and attention layers. The previous code used layer 0's head count as a proxy for the entire model, which returned 0 for these hybrid models. This caused QKV tensor dimensions to collapse to zero, triggering a shape mismatch error. Users of Plamo2 and similar hybrid models can now load them successfully.

**Affected areas:** model

---

### [Bug Fix] — PR #24208: context: fix off-by-one comparisons to n_gpu_layers

**Merged:** 2026-06-06 | **Author:** @CISC

**Summary:** Fixes off-by-one errors in comparisons between `n_gpu_layers` and the actual layer count. The code was comparing against `n_layer` instead of `n_layer_all`, which could cause incorrect GPU offloading decisions for models with extra layers (e.g., MTP heads). Users with models that have extra layers beyond the base model will now get correct layer offloading.

**Affected areas:** context, model

---

### [Bug Fix] — PR #24256: speculative: fix vocab compatibility check

**Merged:** 2026-06-07 | **Author:** @CISC

**Summary:** Fixes a bug where an enum value was being coerced to a boolean before comparison, causing the vocabulary compatibility check between draft and target models to always succeed. This meant incompatible vocabularies could silently produce incorrect speculative decoding results. The fix ensures proper type comparison, preventing incorrect speculative decoding when draft and target model vocabularies don't match.

**Affected areas:** speculative

---

### [Bug Fix] — PR #24234: common/chat: fix LFM2/LFM2.5 reasoning round-trip and `<think>` leak

**Merged:** 2026-06-06 | **Author:** @tdakhran

**Summary:** Fixes issues with the LFM2 and LFM2.5 chat template parsers where reasoning content could leak outside `<think>` tags during chat round-trips. This ensures that model responses correctly separate reasoning from final output when using these chat formats. Users of LFM2/LFM2.5 models benefit from cleaner chat interactions.

**Affected areas:** common, chat

---

### [Bug Fix] — PR #24268: convert: fix conversion for Mistral-Medium-3.5-128B

**Merged:** 2026-06-07 | **Author:** @dfriehs

**Summary:** Fixes the Hugging Face to GGUF converter when processing Mistral-Medium-3.5-128B, where Mistral explicitly sets `moe` and `llama_4_scaling` to `null` in params.json. The previous `key in dict` checks couldn't handle null values, causing a `TypeError` during conversion. The fix replaces these with `dict.get(key) is not None` checks. Users converting this 128B MoE model can now successfully create GGUF files.

**Affected areas:** python, conversion

---

### [Bug Fix] — PR #24242: convert: fix Gemma4 with no audio encoder

**Merged:** 2026-06-07 | **Author:** @CISC

**Summary:** Fixes the GGUF converter for Gemma 4 model variants that don't include an audio encoder. The converter now checks for the existence of audio projector metadata before attempting to add it, preventing conversion failures on text-only Gemma 4 variants. Users converting Gemma 4 models without audio capabilities will now succeed.

**Affected areas:** python, conversion

---

### [Performance] — PR #24277: kv-cache: avoid kv cells copies

**Merged:** 2026-06-07 | **Author:** @ggerganov

**Summary:** Fixes a performance issue where `llama_kv_cells` copies in `apply_ubatch` could become expensive on certain host configurations. This temporary patch avoids the performance hit while a more thorough refactoring is planned. Users with multi-GPU setups or large batch sizes will see improved performance during batch processing.

**Affected areas:** kv-cache, performance

---

### [Performance] — PR #24267: kv-cache: follow the source cache size when sharing cells

**Merged:** 2026-06-07 | **Author:** @ServeurpersoCom

**Summary:** Fixes a crash in speculative decoding when using `--fit` mode, where the trunk context could shrink below the draft model's default. The assistant would then build views sized for its own kv_size into smaller shared K/V tensors, triggering an assertion. Now the shared K/V cache correctly follows the source cache size. Users running speculative decoding with `--fit` on any GPU backend benefit from this fix.

**Affected areas:** kv-cache, speculative

---

### [Server/API] — PR #23913: mtmd, server: add "placeholder bitmap" for counting tokens, add `*/input_tokens` API

**Merged:** 2026-06-06 | **Author:** @ngxson

**Summary:** Adds a "placeholder bitmap" optimization that allows token counting for multimodal inputs without expensive CPU preprocessing. Previously, tokenizing images/audio required full CPU preprocessing on a single thread. Now users can count tokens via a lightweight placeholder. New `*/input_tokens` server API endpoints are added for retrieving input token counts without full processing. Benefits anyone using multimodal inputs through llama-server.

**Affected areas:** server, mtmd, python

---

### [Server/API] — PR #24290: server: reject mismatched embedding batch sizes

**Merged:** 2026-06-08 | **Author:** @zwj110610

**Summary:** Fixes a long-standing issue where `llama-server` silently ignored mismatched `--batch-size` and `--ubatch-size` values when `--embedding` was enabled. The server now exits with a clear error message instead of silently changing `n_batch`. Users must ensure `--batch-size` equals `--ubatch-size` when using embedding mode. A new test validates this startup failure case.

**Affected areas:** server

---

### [CLI/Tooling] — PR #24283: cli: fix spinner not show during prompt processing

**Merged:** 2026-06-08 | **Author:** @ngxson

**Summary:** Fixes a regression introduced by PR #23884 where the spinner animation during prompt processing was not displayed in the CLI. The spinner now correctly animates to indicate progress during the prompt evaluation phase. This is a visual fix only — no functional impact on inference.

**Affected areas:** CLI, examples

---

### [CLI/Tooling] — PR #24239: arg: Skip mmproj download when user supplied mmproj

**Merged:** 2026-06-07 | **Author:** @konradmb

**Summary:** Fixes unnecessary Hugging Face downloads when using `-hf` with multimodal models. Previously, if a user specified `--mmproj` or `--mmproj-url` manually, the system would still download the mmproj file from Hugging Face. Now the download is skipped when an mmproj is already provided, saving bandwidth and time for multimodal users.

**Affected areas:** CLI, common

---

### [CLI/Tooling] — PR #23744: common: relax sampler name matching

**Merged:** 2026-06-07 | **Author:** @ddh0

**Summary:** Relaxes sampler name matching so that alternative names (like `top-k` and `min-p`) are always recognized alongside canonical names (`top_k` and `min_p`). Previously, the `allow_alt_names` flag was not consistently enabled across all call sites, causing some sampler configurations to fail. Users can now use alternative sampler names interchangeably without issues.

**Affected areas:** common, sampling

---

### [Build/CI] — PR #24228: docker: bump cuda13 to 13.3.0

**Merged:** 2026-06-07 | **Author:** @CISC

**Summary:** Bumps the CUDA 13 Docker image from the previous version to CUDA 13.3.0. This ensures Docker-based builds and CI use the latest CUDA toolchain with the newest compiler optimizations and bug fixes. Users building via Docker will automatically get the updated CUDA version.

**Affected areas:** docker, build

---

### [Build/CI] — PR #24070: [SYCL] Update compute runtime version to 26.x in docker

**Merged:** 2026-06-08 | **Author:** @arthw

**Summary:** Updates the Intel compute runtime version used in SYCL Docker builds from 25.x to 26.x for improved performance on Intel GPUs. The old 25.x runtime is kept as a comment for reference, as it can fix multi-GPU issues on some configurations. SYCL users building via Docker will now get better Intel GPU performance by default.

**Affected areas:** SYCL, docker, build

---

### [Other] — PR #24226: completion: remove useless statics

**Merged:** 2026-06-06 | **Author:** @angt

**Summary:** Removes unnecessary static storage class specifiers from variables in the completion example, cleaning up the code with no functional impact. This is a minor code quality improvement.

**Affected areas:** examples, completion

---

### [Other] — PR #24213: completion: fix format specifier in LOG_INF

**Merged:** 2026-06-06 | **Author:** @angt

**Summary:** Fixes a format specifier warning in the completion example where `long` was used but the argument was `unsigned long long`. This resolves a compiler warning but has no user-facing impact.

**Affected areas:** examples, completion

---

### [Other] — PR #24209: model: rename local n_layer_all variable

**Merged:** 2026-06-06 | **Author:** @CISC

**Summary:** Non-functional rename of a local variable for clarity, following the `n_layer` refactor in PR #24060. No user-facing impact.

**Affected areas:** model

---

### [Other] — PR #24178: common/chat: unify and fix LFM2/LFM2.5 tool parser

**Merged:** 2026-06-05 | **Author:** @tdakhran

**Summary:** Merges the LFM2 and LFM2.5 tool-calling parsers into a single shared implementation, since both use the same pythonic-style tool-calling format (differing only in system tool list wrapping). Also fixes argument parsing for Python literals (True/False/None → JSON equivalents). Users of LFM2/LFM2.5 models get more robust tool-calling support.

**Affected areas:** common, chat

---

*Report generated 2026-06-08T09:38:41.176Z | Next update: tomorrow*
